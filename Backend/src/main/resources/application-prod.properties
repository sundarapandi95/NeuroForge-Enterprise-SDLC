# ===================================================================
# Spring Profile: prod (Strict Production Profile)
# ===================================================================

# Database (Strict validation, environment injection required)
spring.datasource.url=jdbc:postgresql://localhost:5432/neurofourge_db
spring.datasource.username=postgres
spring.datasource.password=Root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Security
jwt.secret=${JWT_SECRET}

# Mail Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vmowneesh8@gmail.com
spring.mail.password=drorbsqkgnplwbue
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
